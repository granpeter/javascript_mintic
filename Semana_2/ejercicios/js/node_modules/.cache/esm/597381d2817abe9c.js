let busquedaPorId;_817‍.w("./main",[["busquedaPorId",["busquedaPorId"],function(v){busquedaPorId=v}]]);

const busquedaPorIDAsync = (id) => {

return new Promise ((resolve, reject) => {
    setTimeout(() => {
        const p= busquedaPorId(id);

        if (p){
            resolve(p)
        }else{
            reject ('no se cumplio la promesa - no encontró el heroe');  
        }

    }, 2000);

});

}

busquedaPorIDAsync(11)
.then (tripulante => _817‍.g.console.log ('tripulante',tripulante))
.catch(err => _817‍.g.console.warn(err))